.messages-board-title
  h1 align="center" 
    = t('board_title')
.messages-board
  .medium-6.columns (data-behavior='messages')
    .message-item#new_item
      = render('new_item')

    = render(@messages)

  = link_to_previous_page(@messages, 'Previous Page', class: 'previous-page')
  = link_to_next_page(@messages, 'Next Page', class: 'next-page')
